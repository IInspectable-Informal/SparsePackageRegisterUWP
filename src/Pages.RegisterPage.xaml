<Page 
    x:Class="SparsePackageManager.Pages.RegisterPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SparsePackageManager"
    xmlns:localp="using:SparsePackageManager.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    NavigationCacheMode="Required"
    AllowDrop="True" DragEnter="IdentifyObject" Drop="GetIStorageItem">

    <Page.Resources>
        <ResourceDictionary>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <Color x:Key="WarningForeground">Red</Color>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <Color x:Key="WarningForeground">Yellow</Color>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
        </ResourceDictionary>
    </Page.Resources>

    <ScrollViewer Padding="36,12" VerticalScrollBarVisibility="Auto">
        <StackPanel Spacing="8">
            <StackPanel.ChildrenTransitions>
                <EntranceThemeTransition FromVerticalOffset="100" IsStaggeringEnabled="True"/>
                <RepositionThemeTransition IsStaggeringEnabled="True"/>
            </StackPanel.ChildrenTransitions>
            <muxc:Expander IsExpanded="True" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <muxc:Expander.Resources>
                    <Thickness x:Key="ExpanderChevronMargin">16,0,8,0</Thickness>
                </muxc:Expander.Resources>
                <muxc:Expander.Header>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock x:Uid="BasicArgs" VerticalAlignment="Center"/>
                        <Button Click="Register" Style="{StaticResource AccentButtonStyle}" Grid.Column="1">
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE896;" FontSize="19"/>
                                <TextBlock x:Uid="Register"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                </muxc:Expander.Header>
                <Grid RowSpacing="8" ColumnSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xE7B8;"/>
                    <TextBox x:Name="MSIXPath" x:Uid="MSIXSparsePath" TextWrapping="Wrap" InputScope="Url" VerticalAlignment="Center" Grid.Column="1"/>
                    <Button Click="OpenPicker" Tag="MSIX" Grid.Column="2">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8E5;"/>
                            <TextBlock x:Uid="PickFile"/>
                        </StackPanel>
                    </Button>
                    <FontIcon Glyph="&#xE8B7;" Grid.Row="1"/>
                    <TextBox x:Name="DesktopAppDirPath" x:Uid="DesktopAppPath" TextWrapping="Wrap" InputScope="Url" VerticalAlignment="Center" Grid.Row="1" Grid.Column="1"/>
                    <Button Click="OpenPicker" Tag="Dir" Grid.Row="1" Grid.Column="2">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE8DA;"/>
                            <TextBlock x:Uid="PickDir"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </muxc:Expander>
            <muxc:Expander x:Uid="ExtraArgs" HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch">
                <Grid RowSpacing="6" ColumnSpacing="8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <FontIcon Glyph="&#xF140;"/>
                    <CheckBox x:Name="ForceClose" x:Uid="ForceClose" Grid.Column="1"/>
                    <CheckBox x:Name="ForceUpdate" x:Uid="ForceUpdate" Grid.Row="1" Grid.Column="1"/>
                    <FontIcon Glyph="&#xE730;" Foreground="{ThemeResource WarningForeground}" Grid.Row="2"/>
                    <CheckBox x:Name="AllowUnsignedPackage" x:Uid="AllowUnsigned" Checked="ShowWarning" Grid.Row="2" Grid.Column="1">
                        <ToolTipService.ToolTip>
                            <TextBlock x:Uid="AllowUnsignedTip" TextWrapping="Wrap" HorizontalAlignment="Stretch" Foreground="{ThemeResource WarningForeground}"/>
                        </ToolTipService.ToolTip>
                    </CheckBox>
                </Grid>
            </muxc:Expander>
        </StackPanel>
    </ScrollViewer>

</Page>